# Portfolio — Sample Web App

Two-page responsive portfolio example with an accessible gallery.

Files created:

- index.html — Home page
- gallery.html — Gallery with accessible lightbox
- css/style.css — All styles
- js/main.js — Navigation and small helpers
- js/gallery.js — Gallery lightbox logic

To run: open `index.html` in your browser (no build step required).
